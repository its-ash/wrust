name = "wrust"
main = "build/index.js"
compatibility_date = "2026-03-26"

[build]
command = "curl https://sh.rustup.rs -sSf | sh -s -- -y && . \"$HOME/.cargo/env\" && rustup target add wasm32-unknown-unknown && cargo install -q \"worker-build@^0.7\" && worker-build --release"